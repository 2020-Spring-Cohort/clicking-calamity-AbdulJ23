describe('Testing the functionality of cookie clicker, ()=>{
    it('socred should go up', ()=>{
      let clicker = new clicker();
      let item = {
       title: "score",
       complete: false
     }
      const done = click.clickToAdd(score)
      expect(clickToAdd.getscore().length).toBe(1);
    })
  })

  describe('Testing the functionality of cookie clicker, ()=>{
    it('socred should go up', ()=>{
      let clicker = new clicker();
      let item = {
       title: "score",
       complete: false
     }
      const done = click.clickToAdd(score)
      expect(clickToAdd.getscore().length).toBe(2);
    })
  })


  describe('Testing the functionality of cookie clicker, ()=>{
    it('socred should go up', ()=>{
      let clicker = new clicker();
      let item = {
       title: "score",
       complete: false
     }
      const done = click.clickToAdd(score)
      expect(clickToAdd.getscore().length).toBe(3);
    })
  })


  describe('Testing the functionality of cookie clicker, ()=>{
    it('socred should go up', ()=>{
      let clicker = new clicker();
      let item = {
       title: "score",
       complete: false
     }
      const done = click.clickToAdd(score)
      expect(clickToAdd.getscore().length).toBe(4);
    })
  })
